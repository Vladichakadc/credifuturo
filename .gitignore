# Base de datos (no subir la BD activa local ni copias de respaldo)
# Patrones amplios a propósito: cualquier variante de nombre de copia/backup de la BD
# (sufijos de fecha, .restore, .bak_*, etc.) — un patrón demasiado angosto ya dejó pasar
# database.sqlite.restore y database.sqlite_130626_1100 a un repo público.
*.db
*.db.bak*
*.sqlite
*.sqlite.bak*
*.sqlite.restore
*.sqlite_*
*.sqlite-*
database.sqlite*

# Backups Excel (generados automáticamente por el servidor)
Backups/
*.xlsx
*.xls

# Node modules
node_modules/
**/node_modules/

# Build output
**/dist/
**/build/

# Variables de entorno
.env
**/.env

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# VSCode
.vscode/

# Tokens temporales y archivos transitorios
temp_token.json
*temp_token*

# Scripts de sincronizacion Railway (locales, no deben ir a produccion)
sincronizar_desde_railway_auto.bat
sincronizar_desde_railway.bat

# Skills de Claude Code (herramientas locales, no parte del app)
.claude/skills/
__pycache__/
*.pyc
